<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:WPFSampleCodeEditor">
    <Style TargetType="{x:Type local:CodeEditor}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CodeEditor}">
                    <Grid>
                        <DockPanel>
                            <Border Padding="5,10,0,0" Background="#FFA5F7D2">
                                <StackPanel Width="50" Name="PART_StackPanel"/>
                            </Border>
                            <ScrollViewer x:Name="PART_ContentHost" Padding="5,10,0,0"/>
                        </DockPanel>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!--<Style TargetType="{x:Type Paragraph}">
        <Setter Property="Margin" Value="0"/>
    </Style>-->
</ResourceDictionary>
